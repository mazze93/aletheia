# Only needed for the optional --enrich path (LLM-written reasoning).
# The pure assessment (assess.py) and the hook CLI run with no key and no network.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here
